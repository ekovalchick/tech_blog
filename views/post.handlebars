<div class="text-center">
  <h2>{{title}}</h2>

  <p>{{contents}}</p>
  <p>Created by {{user.username}} on {{format_date date_created}}</p>
  <p>
    <textarea name="" id="update" cols="30" rows="10"></textarea>
    <button class="btn btn-primary" data-id="{{post.id}}">Update</button>

    <textarea name="" id="comment" cols="30" rows="10"></textarea>
    <button class="btn btn-primary" data-id="{{post.id}}">add Comment</button>

  </p>
</div>

<script src="./js/profile.js"></script>